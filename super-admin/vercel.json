{
  "installCommand": "cd .. && npm install",
  "buildCommand": "cd .. && npm run build -w super-admin",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
